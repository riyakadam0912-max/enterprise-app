# 📊 Event Listener System - File Structure & Architecture

## 📁 Complete File Structure

```
api/src/
├── leave-requests/                           # Main module
│   ├── events/
│   │   └── employee-leave-requested.event.ts ✨ (NEW) Event class
│   │
│   ├── listeners/
│   │   └── leave-request-notification.listener.ts ✨ (NEW) Main listener
│   │
│   ├── dto/
│   │   └── create-leave-request.dto.ts       (existing)
│   │
│   ├── leave-requests.module.ts              ✏️ (MODIFIED) Added listeners
│   ├── leave-requests.service.ts             ✏️ (MODIFIED) Emit events
│   ├── leave-requests.controller.ts          (existing)
│   │
│   ├── EVENT_LISTENER_GUIDE.md               ✨ (NEW) Comprehensive docs
│   └── IMPLEMENTATION_PATTERNS.ts            ✨ (NEW) Copy-paste patterns
│
├── mail/                                     # NEW MODULE
│   ├── mail.service.ts                       ✨ (NEW) Mock email service
│   └── mail.module.ts                        ✨ (NEW) Module definition
│
├── app.module.ts                             ✏️ (MODIFIED) Added EventEmitterModule
│
└── ... other modules
```

---

## 🔌 Dependencies Added

`json\n{\n  "dependencies": {\n    "@nestjs/event-emitter": "^2.0.0"  // NEW\n  }\n}\n`

Install with: `npm install @nestjs/event-emitter`

---

## 🎯 How It All Connects

`\n┌─────────────────────────────────────────────────────────────────┐\n│                  APP.MODULE                                      │\n│  ┌─────────────────────────────────────────────────────────┐  │\n│  │ imports: [                                              │  │\n│  │   EventEmitterModule.forRoot()  ← Global event system   │  │\n│  │   LeaveRequestsModule                                   │  │\n│  │   NotificationsModule                                   │  │\n│  │   MailModule                                            │  │\n│  │   ...                                                   │  │\n│  │ ]                                                       │  │\n│  └─────────────────────────────────────────────────────────┘  │\n└─────────────────────────────────────────────────────────────────┘\n                           │\n         ┌─────────────────┼─────────────────┐\n         │                 │                 │\n         ▼                 ▼                 ▼\n\n    ┌─────────────┐   ┌──────────────┐   ┌────────────┐\n    │ LeaveRequest│   │Notifications │   │   Mail     │\n    │   Module    │   │   Module     │   │  Module    │\n    └─────────────┘   └──────────────┘   └────────────┘\n         │                                      │\n    ┌────┴──────────┐                           │\n    │               │                           │\n    ▼               ▼                           ▼\n ┌──────────┐   ┌────────────────────────┐  ┌─────────────┐\n │Service   │   │Listener Provider       │  │MailService  │\n │(emits)   │───│(receives & processes)  │──│(sends email)│\n └──────────┘   └────────────────────────┘  └─────────────┘\n`

---\n\n## 📝 File-by-File Changes\n\n### 1️⃣ Event Class (NEW)\n**File:** `leave-requests/events/employee-leave-requested.event.ts`\n\n`typescript\nexport class EmployeeLeaveRequestedEvent {\n  constructor(\n    public readonly leaveRequestId: number,\n    public readonly employeeId: number,\n    public readonly employeeName: string,\n    public readonly employeeEmail: string | null,\n    public readonly managerId: number | null,\n    public readonly managerName: string | null,\n    public readonly leaveType: string,\n    public readonly startDate: Date,\n    public readonly endDate: Date,\n    public readonly reason?: string,\n  ) {}\n}\n`\n\n**Purpose:** Encapsulates leave request data passed to listeners\n\n---\n\n### 2️⃣ Event Listener (NEW)\n**File:** `leave-requests/listeners/leave-request-notification.listener.ts`\n\n`typescript\n@Injectable()\nexport class LeaveRequestNotificationListener {\n  @OnEvent('employee.leave_requested')  // ← Watches for this event\n  async handleLeaveRequested(\n    event: EmployeeLeaveRequestedEvent\n  ): Promise<void> {\n    // 1. Validate manager exists\n    // 2. Create database notification\n    // 3. Send email to manager\n  }\n}\n`\n\n**Purpose:** Processes leave request events\n\n---\n\n### 3️⃣ Mail Service (NEW)\n**File:** `mail/mail.service.ts`\n\n`typescript\n@Injectable()\nexport class MailService {\n  async sendLeaveRequestNotification(\n    managerEmail: string,\n    managerName: string,\n    employeeName: string,\n    leaveType: string,\n    startDate: Date,\n    endDate: Date,\n    reason?: string,\n  ): Promise<void> {\n    // Mock: logs to console\n    // Production: sends actual email\n  }\n}\n`\n\n**Purpose:** Mock email service (production-ready for replacement)\n\n---\n\n### 4️⃣ Module Updates (MODIFIED)\n**File:** `leave-requests/leave-requests.module.ts`\n\n**Before:**\n`typescript\n@Module({\n  imports: [PrismaModule],\n  providers: [LeaveRequestsService],\n})\nexport class LeaveRequestsModule {}\n`\n\n**After:**\n`typescript\n@Module({\n  imports: [PrismaModule, NotificationsModule, MailModule],\n  providers: [\n    LeaveRequestsService,\n    LeaveRequestNotificationListener,  // ← ADD\n  ],\n})\nexport class LeaveRequestsModule {}\n`\n\n---\n\n### 5️⃣ Service Updates (MODIFIED)\n**File:** `leave-requests/leave-requests.service.ts`\n\n**Added Imports:**\n`typescript\nimport { EventEmitter2 } from '@nestjs/event-emitter';\nimport { EmployeeLeaveRequestedEvent } from './events/employee-leave-requested.event';\n`\n\n**Updated Constructor:**\n`typescript\nconstructor(\n  private readonly prisma: PrismaService,\n  private readonly eventEmitter: EventEmitter2,  // ← ADD\n) {}\n`\n\n**Updated create() method:**\n`typescript\nasync create(dto: CreateLeaveRequestDto, user: AuthUser) {\n  // ... existing code ...\n  \n  // NEW: Emit event if manager exists\n  if (employee.user?.managerId) {\n    const event = new EmployeeLeaveRequestedEvent(...);\n    this.eventEmitter.emit('employee.leave_requested', event);\n  }\n  \n  return leaveRequest;\n}\n`\n\n---\n\n### 6️⃣ App Module (MODIFIED)\n**File:** `app.module.ts`\n\n**Added Import:**\n`typescript\nimport { EventEmitterModule } from '@nestjs/event-emitter';\n`\n\n**Updated Imports Array:**\n`typescript\n@Module({\n  imports: [\n    EventEmitterModule.forRoot(),  // ← ADD\n    PrismaModule,\n    // ... other modules ...\n  ],\n})\nexport class AppModule {}\n`\n\n---\n\n## 🔄 Event Flow (Step by Step)\n\n`\n1. Employee calls POST /leave-requests\n   │\n   ├─ Body: {\n   │   \"startDate\": \"2026-04-15\",\n   │   \"endDate\": \"2026-04-17\",\n   │   \"leaveType\": \"ANNUAL\",\n   │   \"reason\": \"Medical appointment\"\n   │ }\n   │\n   ▼\n2. LeaveRequestsController.create() calls service\n   │\n   ▼\n3. LeaveRequestsService.create()\n   │\n   ├─ Creates leave request in database\n   │\n   ├─ Fetches employee with manager data\n   │\n   ├─ Creates EmployeeLeaveRequestedEvent instance\n   │\n   ├─ Emits: this.eventEmitter.emit('employee.leave_requested', event)\n   │\n   ▼\n4. NestJS Event System receives event\n   │\n   ├─ Finds all listeners with @OnEvent('employee.leave_requested')\n   │\n   ├─ Calls handleLeaveRequested(event)\n   │\n   ▼\n5. LeaveRequestNotificationListener.handleLeaveRequested()\n   │\n   ├─ Checks if managerId exists\n   │\n   ├─ Creates notification:\n   │  └─ this.notificationsService.create({\n   │     userId: managerId,\n   │     title: \"Review Leave Request - John\",\n   │     message: \"...\"\n   │   })\n   │\n   ├─ Sends email:\n   │  └─ this.mailService.sendLeaveRequestNotification(\n   │     managerEmail, managerName, employeeName, ...\n   │   )\n   │\n   ▼\n6. MailService logs mock email to console\n   │\n   ├─ 📧 MOCK EMAIL SENT (Development Mode)\n   ├─ Shows: to, subject, template, context\n   │\n   ▼\n7. Manager gets notified\n   │\n   ├─ Notification appears in dashboard\n   ├─ Email received (in production)\n   │\n   ▼\n8. Response sent to client\n   \n   { \"id\": 1, \"status\": \"PENDING_MANAGER\", ... }\n`\n\n---\n\n## 🎯 Key Design Patterns Used\n\n| Pattern | Implementation | Benefit |\n|---------|---|---|\n| **Event-Driven** | @OnEvent decorator | Decouples service from listeners |\n| **Dependency Injection** | Constructor injection | Easy to mock and test |\n| **Single Responsibility** | Each listener does one thing | Maintainable and testable |\n| **Error Handling** | Try-catch in listeners | Prevents event failures |\n| **Logging** | Logger for debugging | Production visibility |\n| **Type Safety** | Event classes + TypeScript | Prevents bugs at compile time |\n\n---\n\n## 📚 Documentation Files Provided\n\n1. **EVENT_SYSTEM_SUMMARY.md** - Quick reference\n2. **EVENT_LISTENER_GUIDE.md** - Comprehensive guide with examples\n3. **IMPLEMENTATION_PATTERNS.ts** - Copy-paste templates\n4. **THIS FILE** - Architecture diagrams and file structure\n\n---\n\n## ✅ Verification Checklist\n\n- [x] Event class created with proper typing\n- [x] Listener with @OnEvent decorator and handler\n- [x] Mock MailService ready for production replacement\n- [x] Module imports configured correctly\n- [x] Service emits event with proper data\n- [x] EventEmitterModule registered in app.module\n- [x] TypeScript compilation passes (no errors)\n- [x] Error handling implemented\n- [x] Logging added for debugging\n- [x] Documentation complete\n\n---\n\n## 🚀 Next Steps\n\n1. Test by creating a leave request\n2. Check console for mock email output\n3. Verify notification created in database\n4. Add more listeners (Slack, SMS, Calendar, etc.)\n5. Replace mock email with production service\n6. Deploy and monitor\n\n---\n\n## 🎓 Learning Resources\n\n- [NestJS Event Emitter](https://docs.nestjs.com/techniques/events)\n- [Event-Driven Architecture](https://martinfowler.com/articles/201701-event-driven.html)\n- [Observer Pattern](https://refactoring.guru/design-patterns/observer)\n- [Pub-Sub Messaging](https://en.wikipedia.org/wiki/Publish%E2%80%93subscribe_pattern)\n"