import { Link } from 'expo-router';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { CheckSquare, Clock3, FolderKanban } from '@/src/components/icons';
const items = [{ title: 'Tasks', detail: 'Review assignments and priorities', href: '/(app)/tasks', icon: CheckSquare }, { title: 'Projects', detail: 'Track project progress', href: '/(app)/projects', icon: FolderKanban }, { title: 'Timesheets', detail: 'Log and review working time', href: '/(app)/timesheets', icon: Clock3 }];
export default function Work() { return <ScrollView contentContainerStyle={{ padding: 20, backgroundColor: '#f8fafc', flexGrow: 1 }}><Text style={{ fontSize: 28, fontWeight: '800', color: '#172033' }}>Work</Text><Text style={{ color: '#64748b', marginTop: 5, marginBottom: 20 }}>Your operational workspace.</Text>{items.map(({ title, detail, href, icon: Icon }) => <Link key={title} href={href as never} asChild><Pressable style={{ backgroundColor: '#fff', borderRadius: 16, padding: 18, marginBottom: 12, flexDirection: 'row', alignItems: 'center', gap: 14 }}><><View style={{ backgroundColor: '#fff7ed', padding: 12, borderRadius: 12 }}><Icon color="#ea580c" size={22} /></View><View><Text style={{ fontWeight: '700', color: '#172033', fontSize: 16 }}>{title}</Text><Text style={{ color: '#64748b', marginTop: 4 }}>{detail}</Text></View></></Pressable></Link>)}</ScrollView>; }
