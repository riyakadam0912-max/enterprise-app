import { ModuleListScreen, RecordCard } from '@/src/components/ModuleListScreen';
import { employees } from '@/src/api/employees';
import { Link } from 'expo-router';
import { Pressable, Text, View } from 'react-native';
import { useAuth } from '@/src/providers/AuthProvider';
import { can } from '@/src/utils/permissions';
export default function Employees() { const { session } = useAuth(); const canCreate = can(session, 'employee.create', ['HR']); return <View style={{ flex: 1, backgroundColor: '#f8fafc' }}>{canCreate ? <Link href="/(app)/employees/add" asChild><Pressable accessibilityRole="button" style={{ backgroundColor: '#ea580c', margin: 20, marginBottom: 0, padding: 15, borderRadius: 11, alignItems: 'center' }}><Text style={{ color: '#fff', fontWeight: '800' }}>Add employee</Text></Pressable></Link> : null}<ModuleListScreen title="Employees" subtitle="Directory records allowed by your permissions." queryKey={['employees']} load={employees} searchKeys={['name', 'email', 'department', 'designation', 'status']} filters={[{ key: 'status', label: 'Status', options: ['ACTIVE', 'INACTIVE'] }]} sortOptions={[{ key: 'name', label: 'Name' }, { key: 'department', label: 'Department' }]} canAccess={Boolean(session)} renderItem={(item) => <Link href={{ pathname: '/employee/[id]', params: { id: String(item.id) } }} asChild><Pressable accessibilityRole="button"><RecordCard item={item} titleKey="name" detailKeys={['department', 'designation', 'status']} /></Pressable></Link>} /></View>; }
