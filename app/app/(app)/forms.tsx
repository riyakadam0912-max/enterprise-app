import { useQuery } from '@tanstack/react-query';
import { Link } from 'expo-router';
import { Pressable, ScrollView, Text } from 'react-native';
import { apiError } from '@/src/api/client';
import { dynamicForms } from '@/src/api/forms';

export default function Forms() { const query = useQuery({ queryKey: ['dynamic-forms'], queryFn: dynamicForms }); return <ScrollView contentContainerStyle={styles.page}><Text style={styles.title}>Forms</Text><Text style={styles.subtitle}>Available forms for your organization.</Text><Link href="/(app)/form-submissions" asChild><Pressable accessibilityRole="button" style={styles.secondary}><Text style={styles.secondaryText}>View my submissions</Text></Pressable></Link>{query.isLoading ? <Text style={styles.muted}>Loading forms...</Text> : null}{query.isError ? <Text style={styles.error}>{apiError(query.error)}</Text> : null}{!query.isLoading && !query.isError && query.data?.length === 0 ? <Text style={styles.muted}>No forms are available.</Text> : null}{query.data?.map((form) => <Link key={form.id} href={{ pathname: '/form/[id]', params: { id: String(form.id), formName: form.formName } } as never} asChild><Pressable accessibilityRole="button" style={styles.card}><Text style={styles.name}>{form.formName}</Text>{form.description ? <Text style={styles.detail}>{form.description}</Text> : null}<Text style={styles.detail}>{form.targetModule ?? 'General'} · {form.status ?? 'ACTIVE'}</Text></Pressable></Link>)}</ScrollView>; }
const styles = { page: { padding: 20, backgroundColor: '#f8fafc', flexGrow: 1 } as const, title: { color: '#172033', fontSize: 28, fontWeight: '800' } as const, subtitle: { color: '#64748b', marginTop: 5, marginBottom: 18 } as const, secondary: { borderWidth: 1, borderColor: '#cbd5e1', borderRadius: 10, padding: 13, alignItems: 'center', marginBottom: 16 } as const, secondaryText: { color: '#334155', fontWeight: '800' } as const, card: { backgroundColor: '#fff', borderRadius: 14, padding: 16, marginBottom: 10 } as const, name: { color: '#172033', fontWeight: '800', fontSize: 16 } as const, detail: { color: '#64748b', marginTop: 6 } as const, muted: { color: '#64748b', marginTop: 12 } as const, error: { color: '#9f1239', backgroundColor: '#fff1f2', padding: 13, borderRadius: 10, marginBottom: 14 } as const };
